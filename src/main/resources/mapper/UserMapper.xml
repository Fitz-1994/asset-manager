<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assetmanager.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.assetmanager.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="created_at" property="createdAt" typeHandler="com.assetmanager.mapper.InstantTypeHandler"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO users (username, password_hash, created_at)
        VALUES (#{username}, #{passwordHash}, #{createdAt, typeHandler=com.assetmanager.mapper.InstantTypeHandler})
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, username, password_hash, created_at FROM users WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT id, username, password_hash, created_at FROM users WHERE username = #{username}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT id, username, password_hash, created_at FROM users
    </select>
</mapper>
