<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assetmanager.mapper.ManualBalanceMapper">

    <resultMap id="BaseResultMap" type="com.assetmanager.entity.ManualBalance">
        <id column="id" property="id"/>
        <result column="account_id" property="accountId"/>
        <result column="amount" property="amount"/>
        <result column="recorded_at" property="recordedAt" typeHandler="com.assetmanager.mapper.InstantTypeHandler"/>
        <result column="created_at" property="createdAt" typeHandler="com.assetmanager.mapper.InstantTypeHandler"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO manual_balances (account_id, amount, recorded_at, created_at)
        VALUES (#{accountId}, #{amount}, #{recordedAt, typeHandler=com.assetmanager.mapper.InstantTypeHandler}, #{createdAt, typeHandler=com.assetmanager.mapper.InstantTypeHandler})
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
    </insert>

    <select id="findLatestByAccountId" resultMap="BaseResultMap">
        SELECT id, account_id, amount, recorded_at, created_at FROM manual_balances
        WHERE account_id = #{accountId} ORDER BY recorded_at DESC LIMIT 1
    </select>

    <select id="findByAccountIdOrderByRecordedAtDesc" resultMap="BaseResultMap">
        SELECT id, account_id, amount, recorded_at, created_at FROM manual_balances
        WHERE account_id = #{accountId} ORDER BY recorded_at DESC LIMIT #{limit}
    </select>
</mapper>
