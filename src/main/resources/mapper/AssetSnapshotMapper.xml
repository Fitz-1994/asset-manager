<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assetmanager.mapper.AssetSnapshotMapper">

    <resultMap id="BaseResultMap" type="com.assetmanager.entity.AssetSnapshot">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="snapshot_at" property="snapshotAt" typeHandler="com.assetmanager.mapper.InstantTypeHandler"/>
        <result column="trigger_type" property="triggerType"/>
        <result column="total_value_cny" property="totalValueCny"/>
        <result column="created_at" property="createdAt" typeHandler="com.assetmanager.mapper.InstantTypeHandler"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO asset_snapshots (user_id, snapshot_at, trigger_type, total_value_cny, created_at)
        VALUES (#{userId}, #{snapshotAt, typeHandler=com.assetmanager.mapper.InstantTypeHandler}, #{triggerType}, #{totalValueCny}, #{createdAt, typeHandler=com.assetmanager.mapper.InstantTypeHandler})
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
    </insert>

    <select id="findByUserIdOrderBySnapshotAtDesc" resultMap="BaseResultMap">
        SELECT id, user_id, snapshot_at, trigger_type, total_value_cny, created_at FROM asset_snapshots
        WHERE user_id = #{userId} ORDER BY snapshot_at DESC LIMIT #{limit}
    </select>

    <select id="findByUserIdAndSnapshotAtBetweenOrderBySnapshotAtAsc" resultMap="BaseResultMap">
        SELECT id, user_id, snapshot_at, trigger_type, total_value_cny, created_at FROM asset_snapshots
        WHERE user_id = #{userId} AND snapshot_at &gt;= #{from, typeHandler=com.assetmanager.mapper.InstantTypeHandler} AND snapshot_at &lt;= #{to, typeHandler=com.assetmanager.mapper.InstantTypeHandler}
        ORDER BY snapshot_at ASC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, user_id, snapshot_at, trigger_type, total_value_cny, created_at FROM asset_snapshots WHERE id = #{id}
    </select>
</mapper>
