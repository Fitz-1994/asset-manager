<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.assetmanager.mapper.AccountMapper">

    <resultMap id="BaseResultMap" type="com.assetmanager.entity.Account">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="currency" property="currency"/>
        <result column="type" property="type"/>
        <result column="subtype" property="subtype"/>
        <result column="created_at" property="createdAt" typeHandler="com.assetmanager.mapper.InstantTypeHandler"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO accounts (user_id, name, currency, type, subtype, created_at)
        VALUES (#{userId}, #{name}, #{currency}, #{type}, #{subtype}, #{createdAt, typeHandler=com.assetmanager.mapper.InstantTypeHandler})
        <selectKey keyProperty="id" resultType="long" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
    </insert>

    <update id="update">
        UPDATE accounts SET name = #{name}, currency = #{currency}, type = #{type}, subtype = #{subtype}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM accounts WHERE id = #{id}
    </delete>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, user_id, name, currency, type, subtype, created_at FROM accounts WHERE id = #{id}
    </select>

    <select id="findByUserIdOrderById" resultMap="BaseResultMap">
        SELECT id, user_id, name, currency, type, subtype, created_at FROM accounts WHERE user_id = #{userId} ORDER BY id
    </select>
</mapper>
